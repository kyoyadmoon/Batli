# 產品需求文檔 (PRD): 字學 Batli

| 文件資訊 | 內容 |
| --- | --- |
| **產品名稱** | **字學 Batli** |
| **版本** | **v0.1 Web MVP** |
| **文件狀態** | **依目前實作更新** |
| **產品定位** | 讓使用者學習日常生活中常用的中文字的 Web App，特別針對台灣高齡使用者友善設計 |
| **核心價值** | 以大字體、大按鈕、簡化流程、語音輔助與即時回饋，降低長者使用門檻 |
| **技術堆疊** | Web SPA / React + Vite + TypeScript / Web Speech API / Hanzi Writer / localStorage |

---

## 1. 產品目標

### 1.1 問題定義

這個 App 的目標範圍是**讓使用者學習日常生活中常用的中文字**，特別針對需要重新熟悉常見漢字、注音與書寫的台灣高齡使用者。現階段目標不是提供完整教材，而是驗證以下假設：

- 以生活主題切入的三步驟學習流程，是否比傳統課程式列表更容易開始
- 大型卡片、固定 header 與固定底部導航，是否能降低迷失感
- 輔助語言與台語拼音輔助，是否能提升理解與跟讀意願

### 1.2 目前 MVP 範圍

- 平台：手機與平板優先的 Web App
- 主要學習主軸：生活主題識字
- 保留功能：基礎正音（注音）學習與測驗
- 目前不包含：帳號同步、後端、學習歷程雲端保存、圖片資產管理後台

---

## 2. 目前產品結構

### 2.1 全域架構

- 全站使用固定 header
- 左側品牌按鈕回首頁
- 中央主題按鈕顯示目前主題；若沒有目前主題則顯示 `選擇主題`
- 主題按鈕點擊後開啟 overlay，可直接切換生活主題
- 底部固定 `上一頁` / `下一步` 導航，按鈕只顯示 emoji，不顯示說明文字，也不播放語音

### 2.2 目前路由

- `/`：首頁
- `/settings`：設定頁
- `/select`：生活主題選擇頁
- `/zhuyin/learn/:index`：注音學習
- `/zhuyin/quiz`：注音測驗
- `/vocab/:unitId/recognition/:index`：看一看
- `/vocab/:unitId/listening/:index`：聽一聽
- `/vocab/:unitId/writing/:index`：寫一寫

### 2.3 首頁

首頁目前是三個主要入口的儀表板，而不是直接進入主題列表：

- `開始學習`：開啟主題選單 overlay
- `繼續學習`：回到下一個尚未完成的生活字詞
- `設定`：進入設定頁

首頁也會顯示目前設定摘要，例如輔助語言與是否顯示拼音。

---

## 3. 核心互動原則

### 3.1 語音是輔助，不是唯一資訊來源

目前實作並非「零文字、純語音」。產品實際採用的是：

- 重要資訊以大字體直接顯示
- 需要聽讀或朗讀的內容提供語音按鈕
- 題目頁會自動播放提示音或題目語音
- 固定導航箭頭不播放語音，避免過度打擾

### 3.2 一頁一個主要任務

- `看一看`：認字與輔助字詞
- `聽一聽`：聽音選字
- `寫一寫`：筆順示範與描寫
- `設定`：只處理輔助語言與拼音顯示

### 3.3 一致的卡片層級

- 同一層級功能應使用一致的卡片樣式
- `看一看` 的主學習卡、輔助字詞與 `生活詞語` 已統一為同一套卡片語法
- 錯誤回饋採用一致的紅色半透明遮罩與 `X` 動畫

### 3.4 長者導向的低風險互動

- 大型按鈕與高對比
- 導航固定在底部
- 設定變更採「先選擇，再長按 3 秒儲存」以降低誤觸
- 錯誤時不跳頁，保留重試空間

---

## 4. 功能需求

### 4.1 Header 與主題切換

- 全頁固定 header
- 左側品牌按鈕回首頁
- 中央主題按鈕開啟主題 overlay
- overlay 可直接切換主題，也可關閉返回原頁

### 4.2 設定頁

目前設定頁已實作以下功能：

- 輔助語言選擇：
  - 不顯示
  - 中文釋義
  - English
  - 台語
- 當選擇台語時，可用 switch 切換是否顯示拼音
- 設定變更後需長按 3 秒儲存
- 儲存按鈕會以待切換的目標語言預覽文案
- 長按進度環從按鈕上方中央開始

### 4.3 生活主題選擇

- 主題資料由 `vocabularyModule.units` 生成
- 每個主題包含 `icon`、`title`、`voiceIntro`
- 可從首頁 `開始學習` 或 header 主題按鈕進入
- 主題卡支援點擊直接進入，也支援 `🔊` 試聽

### 4.4 看一看（Recognition）

目前 `看一看` 頁包含：

- 主字卡：顯示漢字與注音，點擊可播放主字發音
- 輔助字詞卡：若開啟輔助語言則顯示，點擊可播放輔助語
- 生活詞語區：顯示相關詞、emoji 與輔助語

補充說明：

- 目前未使用圖片型教材卡，主要靠漢字、注音、輔助字詞與相關詞語理解
- `生活詞語` 與上方主卡使用一致的卡片排版

### 4.5 聽一聽（Listening）

目前 `聽一聽` 頁包含：

- 進頁後自動播放題目音
- 每 10 秒可自動重播一次，最多 3 次
- 題目區提供 `再聽一次` 按鈕
- 選項為 2 至 4 個文字卡
- 答對後自動前往 `寫一寫`
- 答錯時播放錯誤音效，並顯示更明確的錯誤動畫：
  - 卡片抖動
  - 半透明紅色遮罩
  - 中央 `X` 動畫
- 可由底部導航直接跳到 `寫一寫`

### 4.6 寫一寫（Writing）

目前 `寫一寫` 頁流程如下：

- 先播放筆順示範
- 示範完成後切入描寫模式
- 第一筆提供語音提示
- 完成描寫後播放正向音效與完成語音
- 使用者可重播筆順
- 可跳過目前字或前往下一字

### 4.7 注音模組

注音仍保留在產品內，但不是首頁主要入口。

#### 注音學習

- 顯示單一注音符號
- 可播放發音
- 可播放筆順示範
- 右下箭頭前往下一個符號
- 左下浮動按鈕返回上一個符號

#### 注音測驗

- 題目自動朗讀
- 選項為注音符號卡片
- 答對後前往下一題，共 5 題
- 答錯時同樣顯示紅色遮罩與 `X` 動畫

### 4.8 學習進度與本機儲存

目前資料保存在瀏覽器 `localStorage`：

- 生活字詞完成進度
- 注音完成進度
- 輔助語言設定
- 是否顯示拼音

目前未提供帳號同步或跨裝置同步。

---

## 5. 回饋機制

### 5.1 正向回饋

- 正確作答播放音效
- 書寫完成播放音效與完成提示

### 5.2 錯誤回饋

- 目前錯誤回饋以音效與動畫為主
- `聽一聽` 與 `注音測驗` 不再額外播放「再試一次」語音提示
- 錯誤視覺效果已明確化，避免只有聲音但看不出狀態改變

---

## 6. 資料來源

| 資源 | 來源 | 授權 |
| --- | --- | --- |
| 台語語音 | [教育部《臺灣台語常用詞辭典》](https://sutian.moe.edu.tw) | [CC BY-ND 3.0 TW](https://creativecommons.org/licenses/by-nd/3.0/tw/) |
| 漢字筆順 | [Hanzi Writer](https://hanziwriter.org/) / [Make Me a Hanzi](https://github.com/skishore/makemeahanzi) | LGPL-3.0 / CC BY-SA 4.0 |
| 中文語音合成 | 瀏覽器 Web Speech API（zh-TW） | 無需授權 |
| 課程內容 | 本專案自行編撰 | — |

